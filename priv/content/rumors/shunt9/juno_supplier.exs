%Shunt.Web.Rumor{
  # TODO: fill in real content for this rumor once the Rumor struct is defined.
  # Shape: id (string), title (string), description (string), source (string),
  # tags (list of strings). Source and tags are display-only — not used by the resolver.
  id: "juno_supplier",
  title: "Juno's Supplier",
  description: "Someone has been supplying Juno with corporate-grade hardware. The shipments don't show up on any manifest.",
  source: "npc",
  tags: ["corporate", "smuggling", "juno"]
}
