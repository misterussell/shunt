%Shunt.Web.RumorConnection{
  id: "bloom_ascent",
  # The believed-cover case: ascension is a harvest dressed as a dream. One rumor
  # (bloom_ascension_ledger) is locked behind the Slate ICE, so a full crack forces a Ghostwork
  # run. Success grants bloom_ascent_clearance — which opens the Uptake AND unlocks the Uptake ICE,
  # where the substrate-truth beneath the cover is finally peeled open.
  rumors: [
    "bloom_ascension_ledger",
    "bloom_vanished_after",
    "bloom_bought_name",
    "bloom_finished_crop",
    "bloom_gloss_shunt",
    "bloom_used_up"
  ],
  partial_threshold: 4,
  success_event_id: "bloom_ascent_success",
  partial_event_id: "bloom_ascent_partial",
  failure_event_id: "bloom_ascent_failure"
}
