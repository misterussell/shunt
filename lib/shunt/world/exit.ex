defmodule Shunt.World.Exit do
  @moduledoc false

  # TODO: define the struct with @enforce_keys [:to] and fields
  # :to, requirements: [], tags: [], travel_text: nil. Add a test in
  # test/shunt/world/exit_test.exs asserting %Exit{} without :to raises
  # ArgumentError, and that %Exit{to: "x"} defaults requirements/tags to []
  # and travel_text to nil.
end
