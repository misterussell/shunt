defmodule Shunt.World.NPC do
  @moduledoc false

  # TODO: define struct per priv/docs/SHUNT_npc_architecture.md "NPC Definition" section:
  # @enforce_keys [:id, :name]
  # defstruct [:id, :name, :description, :location_id, story_arcs: [], repeatable_events: [], services: []]
end
