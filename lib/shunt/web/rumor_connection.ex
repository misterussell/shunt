defmodule Shunt.Web.RumorConnection do
  # TODO: define struct with @enforce_keys [:id, :rumors, :partial_threshold,
  # :success_event_id, :partial_event_id, :failure_event_id] and a plain defstruct.
  # Add fetch!/1 and all/0 delegating to Content.fetch!(:rumor_connections, id)
  # and Content.all(:rumor_connections).
  # Shape must match priv/content/rumor_connections/*.exs files.
end
